# PLAN — 测试计划（第 0 轮产出）

> 第 0 轮生成，后续轮次可追加，不覆盖历史。

## 1. 产品形态与真实入口

- 产品形态：<CLI / API / Web / 库·SDK / 插件 / 后台任务 / 组合>
- 真实入口（用户可达）：
  - <入口 1：如 `npx foo --help` / `GET /api/orders` / 页面路由 `/login`>
  - <入口 2 …>
- 发现依据：<README / package.json scripts / 路由表 / CLI --help / API schema / 导出 / 配置 / 已有测试>

## 2. 启动与基线命令

- 安装：<命令>
- 启动：<命令>
- 构建：<命令>
- 测试：<命令>
- lint / typecheck：<命令>

## 3. 角色与典型旅程

- 角色：<首次用户 / 回访用户 / 管理员 / 访客 …>
- 关键业务旅程：<逐条端到端旅程>

## 4. 场景设计（每功能至少：正常 / 边界 / 误用 / 取消·恢复）

| 功能 | 正常路径 | 边界值 | 错误输入·误用 | 取消/刷新/恢复 | 注意细节 |
|------|---------|--------|--------------|---------------|---------|
| <功能A> | | | | | |

## 5. 测试盲区（能力所限，如实登记）

- <如：无无头浏览器，Web 点按退化为 API+渲染检查>

## 6. 沙箱与环境就绪

- 沙箱方式：<worktree / 分支>
- 种子数据 / 测试凭证：<说明，凭证仅本地测试用>
- 基线检查结果：<构建/测试/lint/typecheck 通过与否；既有失败单独记入 ISSUES>
