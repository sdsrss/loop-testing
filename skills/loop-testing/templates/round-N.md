# Round N — <日期时间>

> 每轮一份，存于 `runs/round-N.md`。记录本轮真实操作、证据与复验重放，供审计与收敛比对。

## 本轮场景（与上轮的实质差异）

- 覆盖内容：<功能与场景概述>
- 与上轮差异：<场景/数据/角色/操作顺序/视口/失败条件 至少一项实质不同>
- 角色画像：<本轮以小白还是暴躁老手为主>

## 真实操作记录（命令 → 结果 → 证据）

```
# 逐条：执行的真实命令 / 操作序列 与 输出摘要
$ <command>
<output 摘要或关键行>
```

## 本轮发现（按级别统计 + ID）

```
new_P0: 0
new_P1: 0
new_P2: 0
new_P3: 0
cases_this_round: 0
```

> 本文件的 `cases_this_round` 是收敛判据 7 的**唯一数字来源**（`references/exit-and-report.md` §1）：
> 一个用例 = `FEATURE_MATRIX.md` 的一行功能 × `PLAN.md` 的一个场景，重复执行只记 1；本轮须
> ≥ 此前最近 3 轮该字段最大值的 80%，否则本轮为「覆盖缩水轮」并把 `converged_streak` 归零。
> 第 1 轮无此前轮次，自动满足。低于阈值而确属合理缩减时，下面一行三样写全且算式成立，否则就是缩水：

- 覆盖缩减说明（仅在低于 80% 时填）：<缩掉哪些用例 + 为何不再适用（功能已删除 / 场景已失效 / 此前轮次重复计数）+ 扣除后的可比基数 B=<数字>；本轮 cases_this_round 须 ≥ B × 80%，后续轮次基线改用 B>
- 新增 ISSUE：<ISSUE-xxx 列表，逐条一句话>

## 修复与复验（修一→验一→提一）

- ISSUE-xxx：修复 commit `<hash>`；复验重放：`<原复现命令>` → <通过/失败>

## 回归结果

- 构建/测试/lint/typecheck：<对比基线，有无新增失败>

## 轮末结算

- 本轮是否收敛低风险轮：<是/否 + 依据>
- converged_streak：<n>/2
- 下轮计划：<重点场景>
