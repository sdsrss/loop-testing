# FINAL_REPORT — loop-testing 最终报告

> 仅在正常收敛停止或触发保险停止时输出。退出序严格按 `references/exit-and-report.md` §4：**① 写完本报告 → ② 把 `STATE.md` 机器 `status:` 写为终态 → ③ 最后才执行 `sandbox-clean`**（停自启进程、清自建临时数据，移除**能确认归属的** worktree——无法确认归属的会被保留并在输出中点名），保留本目录证据。**不要在报告前 clean**：那会先摘掉哨兵 `.active`，留下「哨兵已除、`status:` 仍 RUNNING」的失护窗口。
> 诚实原则：未复验不标 VERIFIED、不夸大覆盖、INCOMPLETE 不得谎报 PASS。

## 1. 最终状态

`FINAL STATUS`: <PASS | CONVERGED_WITH_OPEN_ISSUES | INCOMPLETE | BLOCKED>

- 总轮数：<N>    连续收敛轮数：<n>/2
- 停止原因：<收敛达标 / 达 MAX_ROUNDS / 全部剩余工作被权限阻塞>
- 最后两轮差异与结果：<证明第二收敛轮用了实质不同场景>
- 为何不是更高等级状态：<如为何不是 PASS>

## 2. 覆盖摘要

- 功能总数：<X>；PASS <a> / FAIL <b> / BLOCKED <c> / N/A <d>
- 核心旅程：<逐条列出并标覆盖结果>
- 测试盲区：<能力所限未真实覆盖的部分>

## 3. 已修复问题（ISSUE ↔ commit 对照）

| ISSUE | 级别 | 根因 | 回归断言 | 验证证据 | commit |
|-------|------|------|---------|---------|--------|
| ISSUE-xxx | P1 | | | 重放通过 | `<hash>` |

## 4. 未解决问题（按 P0→P3）

- <逐条：影响 / 复现 / 证据 / 状态 / 不能修的原因 / 建议动作>

## 5. ⚠️ 待用户逐条确认（不合并成模糊问题）

- <每条：现象 / 候选方案 / 推荐方案 / 各方案风险 / 对应 decisions/DEC-xxx.md>

## 6. 既有/环境问题

- <逐条：与本轮改动明确区分——问题在本轮之前就存在，或来自环境/依赖/数据，不是本次改出来的>

## 7. 验证清单

- 实际运行的命令与真实用户场景：<逐条：命令 / 场景 / 结果>
- 未执行项与原因：<逐条；未执行就写未执行，不以「应该可以」代替结果>

## 8. 代码交付与红线声明

- 修改文件：<列表>
- 本地提交：**逐分支列出全部会话期间产生的 commit**——`<分支名>`: `<hash>` `<标题>`；**任何落在主分支上的提交必须在此显式披露**，不得遗漏或合并进其它分支的条目。
- 未提交变更：<列表或无>
- 最终 `git status`：<摘要>
- 红线声明：全程未 push / merge / PR / 发布 / 部署 / force / amend / rebase 到远端；未触碰生产、真实账号、付费接口。
- 声明某文件「未触碰」之前，先用 `git diff` / `git log` 核对：工具附带的修改（例如插件写入的配置）也要如实列出并归因，不得笼统写成「用户自有内容未动」。

## 9. 残余风险与续跑入口

- 残余风险：<简述>
- 证据目录：`docs/looptesting/`
- 续跑入口：<如需继续，从何处开始>
